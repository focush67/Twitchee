import { StreamSkeleton } from "@/components/parts/video-components/stream/index";
const UserLoading = () => {
  return (
    <div className="h-full">
      <StreamSkeleton />
    </div>
  );
};

export default UserLoading;
